#!/usr/bin/env bash
# 占位：浏览器侧验证见 docs/DEPLOY.md「独立 coturn」章节。
# 人工：主播开播后 Console 搜索 [LK:rtc] local candidate，应出现 typ relay 或 srflx。
echo "Open dashboard/watch page and check browser console for [LK:rtc] logs."
